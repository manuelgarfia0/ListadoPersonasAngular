import { Routes } from '@angular/router';
import { ListadoPersonas } from './components/listado-personas/listado-personas';

export const routes: Routes = [
  { path: '', component: ListadoPersonas }
];